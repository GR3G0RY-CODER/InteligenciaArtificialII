"use client";
import Link from "next/link";import { Menu } from "lucide-react";import { useState } from "react";import { Logo } from "@/components/ui/logo";import { Button } from "@/components/ui/button";
export function Navbar(){const [open,setOpen]=useState(false);return <header className="fixed inset-x-0 top-0 z-50 border-b border-slate-200/70 bg-white/90 backdrop-blur-xl"><div className="mx-auto flex h-18 max-w-7xl items-center justify-between px-5 py-4 lg:px-8"><Logo/><nav className="hidden items-center gap-8 md:flex" aria-label="Navegação principal">{[["Produto","#produto"],["Recursos","#recursos"],["Benefícios","#beneficios"]].map(([l,h])=><Link key={l} href={h} className="text-sm font-medium text-slate-600 transition hover:text-slate-950 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand-500">{l}</Link>)}</nav><div className="hidden gap-2 md:flex"><Button asChild variant="ghost"><Link href="/login">Entrar</Link></Button><Button asChild><Link href="/login">Começar agora</Link></Button></div><Button size="icon" variant="ghost" className="md:hidden" aria-label="Abrir menu" onClick={()=>setOpen(!open)}><Menu/></Button></div>{open&&<div className="border-t bg-white p-5 md:hidden"><nav className="flex flex-col gap-4">{["Produto","Recursos","Benefícios"].map(x=><Link key={x} href={`#${x.toLowerCase()}`} onClick={()=>setOpen(false)}>{x}</Link>)}<Button asChild><Link href="/login">Entrar</Link></Button></nav></div>}</header>}
