"use client";
import {useEffect} from "react";import Link from "next/link";import {useRouter} from "next/navigation";import {LogOut} from "lucide-react";import {Logo} from "@/components/ui/logo";import {authService,type UserRole} from "@/services/auth-service";
export function PortalHeader({role,home,allowedRole}:{role:string;home:string;allowedRole:UserRole}){const router=useRouter();useEffect(()=>{void(async()=>{const session=await authService.getSession();if(!session||session.user.role!==allowedRole)router.replace("/login")})()},[allowedRole,router]);return <header className="sticky top-0 z-30 border-b bg-white/90 backdrop-blur"><div className="mx-auto flex h-16 max-w-7xl items-center px-4"><Logo/><span className="ml-3 rounded-full bg-brand-50 px-2.5 py-1 text-xs font-bold text-brand-700">{role}</span><nav className="ml-auto flex items-center gap-4"><Link href={home} className="text-sm font-semibold">Início</Link><Link href="/login" onClick={()=>void authService.logout()} className="flex items-center gap-1 text-sm text-slate-500"><LogOut size={16}/>Sair</Link></nav></div></header>}
